/**
 * skills/index.js – Skill effect utilities re-exported for convenience.
 * Actual execution logic is in actions.js via the ctx (context) pattern.
 */

export { getSkillConfig, isSkillUsable } from '../heroes/index.js';
